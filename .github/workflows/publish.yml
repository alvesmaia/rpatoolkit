name: Publish to NuGet
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
        
      - name: Download NuGet.exe
        run: |
          $nugetUrl = 'https://dist.nuget.org/win-x86-commandline/latest/nuget.exe'
          $nugetPath = '${{ env.HOME }}\nuget.exe'
          Invoke-WebRequest -Uri $nugetUrl -OutFile $nugetPath

      - name: Get Package Version
        id: get_package_version
        run: |
          $version = [xml]::new()
          $version.Load('${{ github.workspace }}\.nuspec')
          $tagVersion = $version.package.metadata.version
          echo "tagVersion=$tagVersion" >> $GITHUB_ENV
          echo "Forma1: $tagVersion"
          
      - name: Passo 1
        id: meu_passo
        run: |
         echo "##[set-output name=VAR_SAIDA]teste"

      - name: Passo 2
        run: echo "Valor de saída ${{ steps.meu_passo.outputs.VAR_SAIDA }}"
          
      - name: Read Package Version
        id: read_package_version
        run: |
          echo "Forma1: ${{ env.tagVersion }}"
          echo "tag_version=$tagVersion" >> $GITHUB_ENV
          
      - name: Use the Package Version
        run: |
          echo "The package version is ${{ env.tag_version }}"
